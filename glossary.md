# Vibecoding 用語集

Vibecodingで必要な最低限の技術用語をまとめた用語集です。難しい専門用語は避け、実際に使う言葉だけを厳選しています。

## 基本概念

### Vibecoding（バイブコーディング）
雰囲気や感覚で、細かい技術詳細を気にせずにコーディングすること。完璧な理解よりも、動くものを作ることを優先する開発スタイル。

### Web（ウェブ）
インターネット上で情報を見たり、アプリを使ったりする仕組み。ブラウザで見られるすべてのもの。

### ブラウザ
WebページやWebアプリを表示するソフト。Chrome、Safari、Firefoxなど。

### コード
コンピュータへの命令を書いた文章。プログラミング言語で書く。

---

## HTML - ページの骨組み

### HTML（エイチティーエムエル）
Webページの構造を作る言語。文章や画像の配置を決める骨組み。

### タグ
HTMLで使う命令の単位。`<タグ名>内容</タグ名>`のように書く。

### 主要なタグ
- `<h1>` ~ `<h6>`: 見出し（数字が小さいほど大きい見出し）
- `<p>`: 段落（文章のかたまり）
- `<div>`: グループ化のための箱
- `<button>`: ボタン
- `<input>`: 入力欄
- `<img>`: 画像
- `<a>`: リンク

### 属性（ぞくせい）
タグに追加情報を与えるもの。`<タグ 属性名="値">`のように書く。
例：`<button id="myButton">クリック</button>`

---

## CSS - ページの見た目

### CSS（シーエスエス）
Webページの見た目を整える言語。色、サイズ、配置などを決める。

### セレクタ
CSSでどの要素にスタイルを適用するか指定するもの。
- `.クラス名`: クラスで指定
- `#id名`: IDで指定
- `タグ名`: タグ名で指定

### プロパティ
見た目の種類。`color`（色）、`font-size`（文字サイズ）など。

### 値
プロパティに設定する具体的な内容。`red`（赤）、`16px`（16ピクセル）など。

### よく使うプロパティ
- `color`: 文字の色
- `background-color`: 背景色
- `font-size`: 文字の大きさ
- `margin`: 外側の余白
- `padding`: 内側の余白
- `display`: 表示方法（`flex`を使うとレイアウトが楽）

---

## JavaScript - ページに動きをつける

### JavaScript（ジャバスクリプト）
Webページに動きをつける言語。ボタンを押したときの処理などを書く。

### 変数（へんすう）
データを保存する箱。`let 変数名 = 値;` で作る。

### 関数（かんすう）
処理をまとめたもの。何度も使える命令セット。
```javascript
function 関数名() {
  // 処理
}
```

### イベント
ユーザーの操作（クリック、入力など）に反応すること。

### イベントリスナー
イベントが起きたときに実行する処理を登録すること。
```javascript
要素.addEventListener('イベント名', 関数);
```

### よく使うイベント
- `click`: クリックしたとき
- `input`: 入力したとき
- `submit`: フォームを送信したとき

### DOM（ドム）
HTMLの要素をJavaScriptから操作できるようにする仕組み。

### 要素の取得
```javascript
document.querySelector('セレクタ') // 1つ取得
document.querySelectorAll('セレクタ') // 全部取得
```

---

## ファイル構成

### index.html
Webサイトの最初のページ。ブラウザが最初に読み込むファイル。

### style.css
スタイル（見た目）をまとめたファイル。

### script.js
JavaScript（動き）をまとめたファイル。

---

## デバッグ - エラーを見つける

### コンソール
ブラウザの開発者ツールにある、エラーメッセージや動作確認ができる場所。

### console.log()
コンソールにメッセージを表示する命令。デバッグに便利。
```javascript
console.log('ここが実行されました');
```

### デベロッパーツール / 開発者ツール
ブラウザに組み込まれた、コードを確認・デバッグするためのツール。
- Chrome: F12キーまたは右クリック→「検証」
- Safari: 環境設定で有効化後、⌘+Option+I

---

## よく使う英単語

### プログラミング用語
- **function**: 関数
- **variable**: 変数
- **element**: 要素
- **style**: スタイル、見た目
- **event**: イベント、出来事
- **click**: クリック
- **add**: 追加する
- **remove**: 削除する
- **get**: 取得する
- **set**: 設定する
- **value**: 値
- **name**: 名前
- **id**: 識別子
- **class**: クラス

### HTML/CSS用語
- **body**: 本体（ページの本文部分）
- **head**: ヘッダー（ページの設定部分）
- **button**: ボタン
- **input**: 入力
- **text**: テキスト、文字
- **image**: 画像
- **link**: リンク
- **color**: 色
- **size**: サイズ
- **width**: 幅
- **height**: 高さ
- **margin**: マージン（外側の余白）
- **padding**: パディング（内側の余白）

---

## Vibecodingの心得

1. **完璧を求めない**: 動けばOK。後から直せる。
2. **コピペも技術**: 動くコードをベースに改造する。
3. **エラーは友達**: エラーメッセージをよく読むと答えが書いてある。
4. **まず動かす**: 理解は後からついてくる。
5. **小さく始める**: 大きな機能は小さく分けて一つずつ作る。

---

## 参考リンク

- [MDN Web Docs](https://developer.mozilla.org/ja/) - 信頼できる技術リファレンス
- [W3Schools](https://www.w3schools.com/) - 初心者向けチュートリアル
- [Can I use](https://caniuse.com/) - ブラウザ対応状況の確認
